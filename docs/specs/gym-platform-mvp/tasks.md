# Plan incremental — Gym Platform MVP

- Estado: Borrador para revisión
- Regla: implementar una tarea por vez y actualizar este archivo al completarla.
- Persistencia: DynamoDB On-Demand como única base principal, con AWS SDK v3 y repositorios propios.

Cada tarea indica objetivo, dependencias, módulos esperados, finalización/pruebas y requisitos relacionados.

## Evidencia de ejecución

| Tarea | Estado | Fecha | Evidencia |
|---|---|---|---|
| TASK-001 | COMPLETADA | 2026-08-02 | `npm ci --offline` instaló 357 paquetes desde `package-lock.json` y auditó 359 sin vulnerabilidades; `npm run check` validó formato (26 archivos), secretos (26 archivos), lint, typecheck y pruebas disponibles; `npm run build` compiló Next.js correctamente. |
| TASK-002 | COMPLETADA | 2026-08-02 | `npm ci --offline` instaló 362 paquetes y auditó 369 sin vulnerabilidades; `npm run check` validó formato/secretos (42 archivos), límites sin ciclos (5 workspaces), lint, typecheck estricto de web y cinco paquetes, y pruebas disponibles; `npm run build` compiló correctamente. |
| TASK-003 | COMPLETADA | 2026-08-02 | La prueba específica validó App Router, Next.js 16.2.12, Tailwind CSS y herencia de TypeScript estricto; `npm run check` pasó formato/secretos (43 archivos), límites, lint, typecheck y 1 prueba; `npm run build` compiló; verificación local a 360 px confirmó cero desbordamiento horizontal y cero errores de consola. |
| TASK-004 | COMPLETADA | 2026-08-02 | Layout, navegación y tokens accesibles implementados; Vitest aprobó 2 pruebas y Playwright aprobó 1 smoke sobre build a 360 px, verificando landmarks, salto al contenido por teclado, contraste ≥ 4,5:1 y ausencia de desbordamiento horizontal; `npm run check`, build y cierre limpio del servidor E2E pasaron. |
| TASK-005 | COMPLETADA | 2026-08-04 | BFF base implementado con errores sanitizados, `correlationId` UUID, respuestas `no-store`, validación JSON y límite de 16 KiB; Vitest aprobó 11 pruebas de contratos y entradas inválidas; Playwright aprobó 2 smoke sobre build, incluido `/api/v1/health`; `npm run check` y build pasaron. |
| TASK-006 | COMPLETADA | 2026-08-04 | CDK inicializado con ambiente tipado y obligatorio para `local`, `development` y `production`, stacks separados y protección de terminación en production; 12 pruebas, `npm run check`, build de infraestructura/web y synth de local/development sin credenciales pasaron; la CLI rechazó contexto de ambiente ausente y no se desplegó ni creó ningún recurso del proyecto. |
| TASK-007 | COMPLETADA | 2026-08-04 | Foundation CDK por ambiente con clave KMS rotada, logs cifrados y retenidos 30/90 días, tags obligatorios, presupuesto mensual USD 5/25/100 con alertas SNS cifradas y rol Lambda limitado al LogGroup exacto; 19 assertions, `npm run check`, builds y synth local/development pasaron. Costo revisado: una CMK por ambiente es el costo fijo principal; Budgets/SNS y logs son variables. No hubo deploy. |
| TASK-008 | COMPLETADA | 2026-08-04 | Next.js 16.2.12 y OpenNext 4.0.2 fijados; un wrapper probado confina esbuild a la raíz del repositorio en Windows, genera el bundle real con webpack y verifica Sharp Linux ARM64v8 para la Lambda ARM64. `npm run build:serverless`, formato, lint, typecheck, 39 pruebas, builds y `cdk synth` local/development con artefactos reales pasaron. El template mantiene S3 privado SSE-S3, CloudFront OAC, URLs Lambda con IAM, roles mínimos y cero DynamoDB. No hubo deploy. |
| TASK-009 | COMPLETADA | 2026-08-05 | DynamoDB Local 2.6.1 se ejecuta aislado mediante Docker Compose, con puerto limitado a `127.0.0.1`, raíz de solo lectura, `/tmp` efímero habilitado únicamente para cargar SQLite, `no-new-privileges` y capacidades eliminadas. `npm.cmd run dynamodb:local:test` creó el contenedor, esperó readiness, creó una tabla efímera On-Demand, ejecutó `TransactWriteItems`, verificó dos elementos con lecturas fuertes y eliminó tabla, contenedor y red; terminó con `DynamoDB Local: test completado.` Cinco pruebas del tooling, formato y lint pasan; las validaciones previas de typecheck, build, 39 pruebas unitarias/integración, 2 E2E y synth local/development permanecen vigentes. No hubo conexión ni despliegue AWS. |
| TASK-010 | COMPLETADA | 2026-08-05 | Catálogo tipado de 19 tipos de elemento, codecs reversibles de PK/SK, claves de GSI1/GSI2, cuatro shards determinísticos y tokens HMAC versionados implementados en `packages/data-access`. Seis pruebas verifican round-trip y snapshot de todos los tipos, lookup de nombre shardeado, claves de índices, estabilidad de shards, rechazo de PII/delimitadores y claves malformadas. Formato, secretos, límites, lint, typecheck global, 45 pruebas y build Next.js pasan. Vitest 4.1.10 ya existente se declaró en el workspace; no hubo llamadas DynamoDB, `Scan`, infraestructura ni despliegue. |
| TASK-011 | COMPLETADA | 2026-08-05 | CDK sintetiza exactamente una tabla única por ambiente con PK/SK string, `GSI1-Operational` y `GSI2-Relationships` sparse `KEYS_ONLY`, facturación `PAY_PER_REQUEST`, cifrado mediante la CMK del ambiente y tags de costo. Production activa PITR, deletion protection y `RETAIN`; local/development mantienen PITR desactivado y `DESTROY`. Seis assertions específicas, formato, secretos, límites, lint, typecheck, 51 pruebas, build web y `cdk synth` de local/development/production pasan. No se agregaron permisos DynamoDB, credenciales ni despliegues. |
| TASK-012 | COMPLETADA | 2026-08-05 | Adaptador AWS SDK v3 y repositorio base implementados con configuración `local`/`development`/`production` validada: sólo local admite endpoint HTTP loopback y credenciales ficticias; remoto usa región explícita y cadena de credenciales estándar. El puerto permite únicamente Get, Query y BatchGet, construye expresiones con nombres/valores, pagina con `LastEvaluatedKey`, limita lotes a 100, reintenta sólo `UnprocessedKeys` y emite errores tipados sanitizados. Se fijaron `@aws-sdk/client-dynamodb` y `@aws-sdk/lib-dynamodb` 3.984.0; npm auditó 612 paquetes sin vulnerabilidades. Veintisiete pruebas específicas, formato, secretos, límites, lint, typecheck, 72 pruebas de workspaces, cinco de tooling, build web y smoke efímero de DynamoDB Local pasan. No hay `Scan`, credenciales reales, cambios de infraestructura ni despliegue. |
| TASK-013 | COMPLETADA | 2026-08-07 | Primitivas persistentes implementadas con hash SHA-256 canónico, `Put` condicional, replay fuerte, conflicto por carga distinta, composición atómica mediante `TransactWriteItems` y TTL sólo `EPHEMERAL`. Pasan 36 pruebas unitarias, dos integraciones concurrentes con DynamoDB Local, 30 assertions CDK, cinco pruebas de tooling, 81 pruebas de workspaces, formato, secretos, límites, lint, typecheck, build y `cdk synth` de development. Next.js 16.3.0 corrigió vulnerabilidades runtime previas y Vite 7.3.3 quedó fijado como tooling reproducible. `npm audit` conserva una vulnerabilidad baja de esbuild y dos altas de Vite/CDK sin versión compatible publicada; el usuario aprobó la excepción temporal documentada en `docs/security/dependency-risk-acceptance-2026-08-07.md`. No se agregó `Scan`, secreto, despliegue ni funcionalidad de `TASK-014`. |
| TASK-014 | COMPLETADA | 2026-08-08 | Perfil de usuario, mapeo inmutable de Cognito y lookups HMAC versionados de correo y prefijos de nombre implementados sobre la tabla única. El alta `PENDING`/`STUDENT`, la actualización optimista, la unicidad de correo y el mantenimiento de vistas se ejecutan mediante `TransactWriteItems`; las lecturas canónicas son fuertes, el estado usa cuatro consultas a `GSI1-Operational` y la búsqueda por nombre usa cuatro shards por versión sin `Scan`. Pasan formato (105 archivos), secretos (109), límites de cinco paquetes, lint, typecheck estricto, cinco pruebas de tooling, 84 pruebas unitarias/workspace, cinco integraciones con DynamoDB Local —incluida concurrencia por correo— y build Next.js. No se agregaron dependencias, credenciales, infraestructura ni despliegues. |
| TASK-015 | COMPLETADA | 2026-08-08 | Repositorios tipados de membresías y pagos implementados sobre la tabla única. La creación de membresía mantiene canónico, puntero activo y vistas de vencimiento/estado mediante `TransactWriteItems`, impidiendo dos membresías activas concurrentes. Los pagos mantienen canónico y vistas de fecha/estado con idempotencia durable; la anulación conserva el pago, crea `PaymentCorrection`, cambia la vista de estado y rechaza reuso incompatible de la clave. Historiales usan `Query`; consultas administrativas hacen fan-out de cuatro shards, resuelven vistas y revalidan canónicos con lectura fuerte, sin `Scan`. Pasan formato (111 archivos), secretos (115), límites de cinco paquetes, lint, typecheck estricto, cinco pruebas de tooling, 87 pruebas unitarias/workspace, nueve integraciones con DynamoDB Local —incluidos concurrencia e idempotencia— y build Next.js. No se agregaron dependencias, infraestructura, credenciales ni despliegues. |
| TASK-016 | COMPLETADA | 2026-08-08 | Repositorios tipados de sesiones y reservas implementados sobre la tabla única. Las sesiones se crean transaccionalmente tras comprobar entrenador/tipo activos y mantienen las claves de `GSI1-Operational` por fecha/disponibilidad y `GSI2-Relationships` por entrenador. Las consultas por ID, fecha, entrenador, periodo disponible, clase y alumno usan `GetItem`, `Query` y `BatchGetItem` fuerte, con fan-out acotado y sin `Scan`. La reserva expone un `Put` condicional componible con unicidad natural `CLASS#{classId}/RESERVATION#{studentId}`; el cupo se prepara desde lectura fuerte y produce un `Update` condicionado por estado, versión, contador y capacidad. Una integración con dos intentos simultáneos para un cupo confirma exactamente uno, contador final uno y una sola reserva. Pasan formato (117 archivos), secretos (121), límites de cinco paquetes, lint, typecheck estricto, cinco pruebas de tooling, 91 pruebas unitarias/workspace, 12 integraciones con DynamoDB Local y build Next.js. No se implementó aún el workflow completo/idempotente de `TASK-036`; no se agregaron dependencias, infraestructura, credenciales ni despliegues. |
| TASK-017 | COMPLETADA | 2026-08-08 | Modelos y repositorios tipados de galería/consentimiento, notificaciones, configuración y auditoría implementados sobre la tabla única. La publicación usa `TransactWriteItems` con versión, estado `READY`, consentimiento `GRANTED` vigente y vista pública sin la clave del original; recordatorios usan deduplicación persistente y cuatro consultas a `GSI1-Operational`; settings usa lectura fuerte y versión condicional; auditoría es append-only, rechaza campos sensibles y consulta por entidad o actor/fecha mediante PK/GSI2. Un guard explícito rechaza `ScanCommand`. Pasan formato (125 archivos), secretos (129), límites de cinco paquetes, lint, typecheck estricto, cinco pruebas de tooling, 96 pruebas unitarias/workspace, 15 integraciones con DynamoDB Local —incluidos replay, conflicto idempotente y consentimiento revocado—, dos E2E y build Next.js. No se agregaron dependencias, infraestructura, credenciales ni despliegues. |
| TASK-018 | COMPLETADA | 2026-08-08 | Cognito quedó definido por ambiente con un User Pool social-only en plan Lite, cliente público sin secreto, Hosted UI y OAuth authorization-code para Google/Facebook. Local restringe callback/logout a `localhost:3000`; development/production derivan HTTPS del dominio CloudFront de su stack. IDs y secretos OAuth se resuelven desde referencias dinámicas de Secrets Manager separadas por ambiente; no se crean ni almacenan valores reales. Atributos de email/nombre son mínimos, tokens duran 15 minutos, revocación está activa, production retiene/protege el pool y el runtime no recibe permisos Cognito. Pasan formato (128 archivos), secretos (132), límites, lint, typecheck estricto, cinco pruebas de tooling, 104 pruebas unitarias/workspace —incluidas ocho assertions Cognito—, build de infraestructura/web y `cdk synth` local/development. No hubo dependencias nuevas, `cdk deploy`, recursos remotos ni implementación de `TASK-019`. |
| TASK-019 | COMPLETADA | 2026-08-08 | Rutas de login/callback y servicio OAuth implementados con authorization code, PKCE S256, `state` y `nonce` aleatorios y comparación segura. El callback intercambia el código con timeout, verifica criptográficamente el ID token contra JWKS y exige emisor, audiencia, `token_use=id`, nonce y correo verificado antes del alta transaccional `PENDING`/`STUDENT`; el mapeo Cognito y condiciones de DynamoDB hacen idempotente el callback repetido. La sesión de 15 minutos y las cookies transitorias son `HttpOnly`, `SameSite=Lax`, `Secure` fuera de local y sin `Domain`; los errores no exponen tokens. La configuración pública se consulta por cinco parámetros SSM y el HMAC se genera/cifra en Secrets Manager; el runtime sólo recibe `GetItem`, `TransactWriteItems`, `GetParameters` y lectura del secreto sobre recursos exactos, sin `Scan`. Se agregaron `jose` 6.2.8 y clientes AWS SDK v3 SSM/Secrets Manager 3.984.0. Pasan `npm run check` —formato (139), secretos (143), límites, lint, typecheck, cinco pruebas de tooling y 111 pruebas workspace—, 15 integraciones con DynamoDB Local, tres E2E —incluido redirect PKCE—, build, prueba standalone health/login y `cdk synth` local/development. `npm audit` conserva sólo la excepción temporal ya aprobada para Vite/esbuild/CDK; no hubo secretos reales, `cdk deploy`, recursos remotos ni inicio de `TASK-020`. |
| TASK-020 | COMPLETADA | 2026-08-08 | Onboarding propio y logout implementados. El backend verifica firma/vigencia del ID token, resuelve el `userId` exclusivamente mediante el mapeo Cognito y sólo permite finalizar perfiles `PENDING`; nombre y teléfono E.164 se validan mediante lista blanca y los campos protegidos del cliente se rechazan. DynamoDB usa lectura fuerte y `TransactWriteItems` condicionado por versión, estado y ausencia de finalización; conserva `STUDENT`/`PENDING`, mantiene lookups HMAC de nombre y hace idempotente el replay concurrente sin `Scan`. Logout exige origen aprobado, borra la cookie `HttpOnly` y redirige al endpoint Cognito. La interfaz `/onboarding` es mobile-first, accesible y contempla carga, sesión expirada, errores, doble envío y éxito. Pasan `npm run check` —formato (145), secretos (149), límites, lint, typecheck, cinco pruebas de tooling, cuatro foundation, 26 web, 51 data-access y 39 CDK—, 16 integraciones con DynamoDB Local, seis E2E y build Next.js con las rutas nuevas. No se agregaron dependencias, infraestructura, secretos, `Scan`, despliegues ni alcance de `TASK-021`. |
| TASK-021 | COMPLETADA | 2026-08-09 | Catálogo cerrado de 29 acciones, matriz explícita para `STUDENT`, `STAFF` y `ADMIN`, restricciones por estado y comprobación obligatoria de propiedad implementados en dominio. El evaluador deniega por defecto sesión ausente, acción desconocida, principal inválido, estado, rol, contexto de ownership faltante y recurso ajeno; el guard emite un error tipado y genérico sin identificadores. Trece pruebas cubren la matriz completa, `PENDING`, `SUSPENDED`, `INACTIVE`, `REJECTED`, acceso horizontal y escalación de privilegios. Pasan formato (148 archivos), secretos (152), límites, lint, typecheck estricto, cinco pruebas de tooling, 133 pruebas de workspaces, 16 integraciones con DynamoDB Local y build de web/infraestructura. Vitest 4.1.10 ya existente se declaró en `packages/domain`; no hubo cambios de persistencia, `Scan`, infraestructura, secretos, despliegues ni alcance de `TASK-022`. |
| TASK-022 | COMPLETADA | 2026-08-09 | Scope reutilizable de repositorio implementado en backend: toda operación propia deriva `userId` del principal verificado, exige una acción `*_OWN` y entrega al adaptador un owner no controlable por el cliente; IDs de recurso inválidos, inexistentes o ajenos producen el mismo error genérico. Onboarding usa el guard de `TASK-021` y limita lectura/escritura mediante claves hash del principal; login, callback y logout usan claves hash de red. El limitador valida políticas, normaliza la dirección CloudFront sin puerto, conserva contadores activos y falla cerrado al alcanzar su capacidad, evitando reinicios de cuota. Pruebas negativas cubren ausencia de sesión, IDOR, no enumeración, `STUDENT`, `STAFF`, `ADMIN`, `PENDING`, `SUSPENDED`, `INACTIVE`, escalación, cuotas/reset y saturación. Pasan formato (151 archivos), secretos (155), límites, lint, typecheck estricto, cinco pruebas de tooling, 144 pruebas de workspaces, 16 integraciones con DynamoDB Local, seis E2E y build web/infraestructura. Se declaró la dependencia interna `@gym-adr/domain` en web; no hubo nuevos paquetes externos, `Scan`, índices, IAM, infraestructura, secretos ni despliegues. |
| TASK-023 | COMPLETADA | 2026-08-09 | Sitio público mobile-first implementado con inicio, gimnasio, entrenadores, instalaciones, equipamiento, planes/precios, horarios, galería, ubicación, contacto, redes, WhatsApp configurable y solicitud de ingreso. El contenido central tipado evita inventar datos comerciales: valores aún no confirmados se muestran como consulta pendiente; WhatsApp y redes sólo aceptan configuración HTTPS/Paraguay validada. La navegación por teclado conserva landmarks en móvil/escritorio, los enlaces externos usan `noopener noreferrer` y la galería expone un estado vacío accesible hasta `TASK-042`. Pasan formato (161 archivos), secretos (165), límites, lint, typecheck estricto, cinco pruebas de tooling, 147 pruebas unitarias/workspace, 16 integraciones con DynamoDB Local, nueve E2E responsive y build Next.js de 14 rutas. No se agregaron dependencias, AWS, `Scan`, IAM, secretos ni despliegues. |
| TASK-024 | COMPLETADA | 2026-08-09 | SEO y entrega pública optimizados: metadata descriptiva y canonical por ruta, Open Graph, sitemap limitado a siete páginas públicas, robots que excluye API/portal/administración y manifest en español. La URL pública exige HTTPS salvo loopback local; páginas públicas usan ISR y caché CloudFront de una hora con stale-while-revalidate, mientras onboarding y todos los Route Handlers conservan `no-store`. Next Image queda preparado para AVIF/WebP y tamaños desde 360 px sin habilitar hosts remotos. El E2E mide y exige LCP local ≤2,5 s, navegación <5 s, HTML <100 KiB y recursos críticos <750 KiB. Pasan formato (167 archivos), secretos (171), límites, lint, typecheck estricto, cinco pruebas de tooling, 152 pruebas unitarias/workspace, 16 integraciones con DynamoDB Local, 12 E2E y build Next.js de 17 rutas. No se agregaron dependencias, datos privados a caché, infraestructura, `Scan`, IAM, secretos ni despliegues. |
| TASK-025 | COMPLETADA | 2026-08-09 | Perfil propio implementado en `/me/profile` y `GET/PATCH /api/v1/me/profile`. El backend deriva el `userId` exclusivamente de la sesión verificada, aplica `PROFILE_READ_OWN`/`PROFILE_UPDATE_OWN`, acepta una lista blanca exacta de nombre, teléfono, preferencia de correo y versión esperada, y nunca permite modificar email, roles, estado o fecha de ingreso. La actualización usa lectura fuerte y `TransactWriteItems` con versión optimista, conserva atómicamente el lookup de nombre y devuelve conflictos sanitizados; la preferencia nueva mantiene compatibilidad con perfiles previos. La interfaz en español es mobile-first, accesible y cubre carga, onboarding incompleto, error, éxito y doble envío. Pasan formato (171 archivos), secretos (175), límites, lint, typecheck estricto, cinco pruebas de tooling, 160 pruebas unitarias/workspace, 17 integraciones con DynamoDB Local —incluida concurrencia—, 15 E2E, build Next.js de 19 rutas y `cdk synth` de development. No se agregaron dependencias, `Scan`, IAM, secretos, despliegues ni alcance de `TASK-026`. |
| TASK-026 | COMPLETADA | 2026-08-09 | Gestión de alumnos implementada en `/admin/students`, `GET /api/v1/admin/students` y `GET/PATCH /api/v1/admin/students/{id}`. Pendientes/estado consultan cuatro shards de `GSI1-Operational`, correo usa lookup HMAC exacto y nombre usa prefijos HMAC shardeados; los cursores están acotados, vinculados al filtro y validados antes de llegar a DynamoDB. `STAFF` activo recibe solo campos operativos; únicamente `ADMIN` activo busca por correo, consulta datos completos y ejecuta las transiciones aprobadas. Cada transición relee fuerte, impide auto-modificación, exige versión/motivo cuando corresponde y actualiza perfil, vista de estado y `AuditLog` append-only en un solo `TransactWriteItems`; la concurrencia produce un único ganador y una sola auditoría. IAM añade solo `BatchGetItem`/`Query` sobre tabla y `Query` sobre sus índices, conservando `GetItem`/`TransactWriteItems`, sin comodines. Pasan formato (177 archivos), secretos (181), límites, lint sin advertencias, typecheck estricto, cinco pruebas de tooling, 171 pruebas unitarias/workspace, 19 integraciones con DynamoDB Local, 18 E2E, build Next.js de 21 rutas y `cdk synth` development. No se agregaron paquetes externos, `Scan`, secretos, despliegues ni alcance de `TASK-027`. |
| TASK-027 | COMPLETADA | 2026-08-09 | CRUD lógico de planes implementado en `/admin/plans`, `GET/POST /api/v1/admin/plans` y `GET/PATCH /api/v1/admin/plans/{id}`. El canónico `PLAN#{id}/METADATA` proyecta estado, shard y nombre normalizado en `GSI1-Operational`; los listados hacen fan-out acotado, paginan sin repetir shards agotados y revalidan con `BatchGetItem` fuerte, sin `Scan`. Crear, editar y desactivar usan versión optimista y escriben el canónico más `AuditLog` append-only en un solo `TransactWriteItems`; nunca eliminan el plan ni alteran membresías históricas. `STAFF` activo solo consulta y únicamente `ADMIN` activo muta; sesión ausente y estados no activos se deniegan en backend. Pasan formato (185 archivos), secretos (189), límites, lint, typecheck estricto, cinco pruebas de tooling, 174 pruebas unitarias/workspace, 23 integraciones con DynamoDB Local —incluidos concurrencia, paginación e historial—, 21 E2E responsive, build Next.js de 23 rutas y `cdk synth` development. No se agregaron dependencias, índices, permisos IAM, secretos, `Scan`, despliegues ni alcance de `TASK-028`. |
| TASK-028 | COMPLETADA | 2026-08-09 | Gestión de membresías implementada en `/admin/memberships`, `GET/POST /api/v1/admin/memberships` y `PATCH /api/v1/admin/memberships/{id}`. La creación congela un plan activo y comprueba atómicamente alumno `ACTIVE/STUDENT`; las transiciones aprobadas mantienen canónico, puntero activo con fechas epoch, vistas de vencimiento/estado y `AuditLog` append-only en un solo `TransactWriteItems`, con versión optimista y un único puntero activo. `STAFF` activo crea/edita únicamente membresías pendientes; solo `ADMIN` activo cambia estados, con motivo obligatorio para suspensión/cancelación. Vigencia, próxima y mora usan el día calendario inclusivo de `America/Asuncion`. Pasan formato (192 archivos), secretos (196), límites, lint, typecheck estricto, cinco pruebas de tooling, 179 pruebas unitarias/workspace, 24 integraciones con DynamoDB Local —incluidos conflicto concurrente, ciclo del puntero y auditoría—, 24 E2E responsive y build Next.js de 25 rutas. No se agregaron dependencias, infraestructura, permisos IAM, `Scan`, secretos, despliegues ni alcance de `TASK-029`. |
| TASK-029 | COMPLETADA | 2026-08-09 | Vista propia implementada en `/me/membership` y `GET /api/v1/me/membership`; el backend deriva siempre el alumno de la sesión, consulta puntero/historial con consistencia fuerte y vincula el cursor al propietario, impidiendo IDOR. Reportes administrativos implementados en `/admin/membership-reports` y `GET /api/v1/admin/membership-reports` para vencimiento exacto o estado: hacen `Query` paginado sobre cuatro shards de `GSI1-Operational`, marcan shards agotados, resuelven vistas y releen canónicos con `BatchGetItem` fuerte, sin `Scan`. Cursores opacos, acotados y vinculados al filtro impiden cambiar fecha/estado entre páginas. `STAFF`/`ADMIN` activos consultan; estudiantes, sesión ausente y estados no activos se deniegan en backend. Pasan formato (199 archivos), secretos (203), límites, lint, typecheck estricto, cinco pruebas de tooling, 183 pruebas unitarias/workspace, 24 integraciones con DynamoDB Local, 27 E2E responsive y build Next.js de 29 rutas. No se agregaron dependencias, infraestructura, IAM, secretos, despliegues ni alcance de `TASK-030`. |
| TASK-030 | COMPLETADA | 2026-08-09 | Registro manual implementado en `/admin/payments`, `POST /api/v1/admin/payments` y `POST /api/v1/admin/payment-receipts`. `STAFF`/`ADMIN` activos se autorizan en backend; alumno, membresía y periodo se revalidan, y una clave idempotente acotada al actor produce identificadores estables. Alumno/membresía, canónico, vistas de fecha/estado, idempotencia durable y `AuditLog` append-only se confirman en un solo `TransactWriteItems`, sin `Scan`; reintentos con igual carga reproducen el resultado y una carga distinta entra en conflicto. El comprobante opcional admite PDF/JPG/PNG de hasta 5 MB, usa PUT S3 privado firmado por cinco minutos con cifrado SSE-S3 y un adaptador local firmado/de un solo uso; la UI reutiliza el comprobante ya cargado ante reintentos y previene doble envío. Se añadieron únicamente `@aws-sdk/client-s3` y `@aws-sdk/s3-request-presigner` v3 para la firma oficial. Pasan lint, typecheck estricto, 52 pruebas unitarias de persistencia, 77 pruebas unitarias web/validación/firma, 24 integraciones con DynamoDB Local y build Next.js de 32 rutas. No se agregó infraestructura, IAM, secretos, despliegues ni alcance de `TASK-031`. |
| TASK-031 | COMPLETADA | 2026-08-09 | Historial propio implementado en `/me/payments` y `GET /api/v1/me/payments`; el backend deriva siempre el alumno de la sesión, usa `Query` fuerte sobre `USER#{id}/PAYMENT#...`, liga el cursor al propietario y no expone `receiptKey`. El comprobante propio se autoriza contra el pago canónico y se entrega mediante URL GET firmada por cinco minutos; el adaptador local conserva el objeto privado y emite enlaces de un solo uso. La consulta administrativa en `/admin/payments` usa `GET /api/v1/admin/payments` con filtro por fecha o estado, cuatro shards de `GSI1-Operational`, `BatchGetItem` fuerte y cursores ligados al filtro; shards agotados quedan marcados para no reiniciarlos. `STAFF`/`ADMIN` activos consultan; estudiantes, cursores IDOR y estados no autorizados se deniegan en backend. Pasan lint, typecheck estricto, 52 pruebas unitarias de persistencia, 80 pruebas unitarias web/validación/autorización/firma, 24 integraciones con DynamoDB Local y build Next.js de 35 rutas. No se agregaron dependencias, infraestructura, IAM, secretos, despliegues, `Scan` ni alcance de `TASK-032`. |
| TASK-032 | COMPLETADA | 2026-08-09 | Anulación, ajuste y compensación implementados en `POST /api/v1/admin/payments/{id}/corrections` y `/admin/payments`. Solo `ADMIN` activo recibe la capacidad y supera la autorización backend; la versión del original se comprueba con lectura fuerte y condición transaccional. La anulación conserva el canónico como `VOIDED`; ajustes y compensaciones crean pagos confirmados separados con `originalPaymentId` y un `PaymentCorrection` enlazado. Canónico/vistas, corrección, idempotencia durable y `AuditLog` append-only se escriben en un único `TransactWriteItems`; los reintentos recuperan el resultado original y cargas incompatibles fallan. Pasan lint, typecheck estricto, 52 pruebas unitarias de persistencia, 82 pruebas unitarias web/validación/autorización, 25 integraciones con DynamoDB Local y build Next.js con la nueva ruta. No se agregaron dependencias, infraestructura, IAM, secretos, despliegues, `Scan` ni alcance de `TASK-033`. |
| TASK-033 | COMPLETADA | 2026-08-09 | CRUD lógico de entrenadores y tipos de clase implementado en `/admin/class-catalog`, `GET/POST /api/v1/admin/trainers`, `PATCH /api/v1/admin/trainers/{id}`, `GET/POST /api/v1/admin/class-types` y `PATCH /api/v1/admin/class-types/{id}`. Los canónicos `TRAINER#{id}` y `CLASS_TYPE#{id}` mantienen identificadores estables y vistas shardeadas por estado en `GSI1-Operational`; listados usan `Query` y `BatchGetItem` fuerte, sin `Scan`. Crear, editar y desactivar escriben canónico y `AuditLog` append-only en un único `TransactWriteItems` con versión optimista; la baja es lógica y las sesiones conservan nombres/IDs congelados. `STAFF` activo solo consulta y únicamente `ADMIN` activo muta. Pasan lint, typecheck estricto, 52 pruebas unitarias de persistencia, 85 pruebas unitarias web/validación/autorización, 26 integraciones con DynamoDB Local y build Next.js de 38 páginas generadas con cinco rutas nuevas. No se agregaron dependencias, infraestructura, IAM, secretos, despliegues, `Scan` ni alcance de `TASK-034`. |
| TASK-034 | COMPLETADA | 2026-08-09 | Creación y edición de sesiones concretas implementadas en `/admin/class-sessions`, `GET/POST /api/v1/admin/class-sessions` y `PATCH /api/v1/admin/class-sessions/{id}`. El backend autoriza `STAFF`/`ADMIN` activos, deriva fecha y hora en `America/Asuncion`, congela nombres de entrenador/tipo activos y valida entradas estrictas. Cada mutación usa un único `TransactWriteItems` con referencias activas, versión optimista, condición de capacidad no inferior a reservas confirmadas y `AuditLog` append-only; fecha/disponibilidad en `GSI1-Operational` y entrenador en `GSI2-Relationships` se actualizan con el canónico, sin `Scan`. Pasan lint, typecheck estricto, 53 pruebas unitarias de persistencia, 88 pruebas unitarias web/validación/autorización, 26 integraciones con DynamoDB Local y build Next.js de 40 rutas. No se agregaron dependencias, infraestructura, IAM, secretos, despliegues ni alcance de `TASK-035`. |
| TASK-035 | COMPLETADA | 2026-08-09 | Cancelación administrativa implementada en `/admin/class-sessions` y `POST /api/v1/admin/class-sessions/{id}/cancellation`. La primera transacción condiciona estado/versión, cambia inmediatamente la sesión a `CANCELLED`, elimina su disponibilidad reservable, registra auditoría y conserva idempotencia durable ligada al actor. La propagación consulta `PK=CLASS#{id}` con `Query` fuerte en lotes de 20, cambia reservas `CONFIRMED` a `ADMIN_CANCELLED`, decrementa contador/versiona la sesión y audita cada lote dentro de un único `TransactWriteItems`; cursores ligados a sesión/solicitud permiten reanudar y los reintentos no duplican efectos. `STAFF`/`ADMIN` activos se autorizan en backend y `STUDENT` queda denegado; la UI exige motivo, evita doble envío y conserva el cursor mientras reanuda. Pasan lint, typecheck estricto, 54 pruebas unitarias de persistencia, 89 pruebas unitarias web/validación/autorización, 27 integraciones con DynamoDB Local —incluido un flujo de 21 reservas en dos lotes y replays— y build Next.js con la nueva ruta. No se agregaron dependencias, infraestructura, IAM, secretos, despliegues, `Scan` ni alcance de `TASK-036`. |

## Fase 1. Fundación del repositorio

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-001 | Completar monorepo npm, Node fijado y scripts raíz. | Aprobación | raíz/config | Instalación reproducible; scripts y secret scan pasan. | REQ-SEC-003/004 |
| TASK-002 | Crear límites de paquetes. | TASK-001 | `packages/{domain,data-access,validation,shared}`, `infrastructure` | Imports por workspace, strict y anti-ciclos verificados. | REQ-SEC-001 |

## Fase 2. Aplicación Next.js

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-003 | Validar Next.js App Router, TypeScript strict y Tailwind instalados. | TASK-002 | `apps/web` | Build/lint/typecheck; página a 360 px. | REQ-PUBLIC-001 |
| TASK-004 | Crear layout, navegación, tokens y accesibilidad base. | TASK-003 | app/components/styles | Teclado, foco, contraste; Vitest/Playwright smoke. | REQ-PUBLIC-001, REQ-SEC-002 |
| TASK-005 | Establecer BFF, errores, validación y correlation IDs. | TASK-003 | server/validation | Contratos y entradas inválidas probados. | REQ-SEC-001/002 |

## Fase 3. Infraestructura base

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-006 | Inicializar CDK y configuración tipada local/dev/prod. | TASK-001 | `infrastructure` | `cdk synth` sin credenciales/despliegue; ambiente explícito. | REQ-SEC-004 |
| TASK-007 | Definir cifrado, logs, tags, budgets y roles mínimos base. | TASK-006 | stacks security/foundation | Assertions de cifrado/IAM/tags; costo revisado. | REQ-SEC-003/004 |
| TASK-008 | Sintetizar hosting Next.js serverless sin desplegar. | TASK-003, TASK-006 | web stack | Template y compatibilidad del adaptador probados. | REQ-PUBLIC-001, REQ-SEC-004 |

## Fase 4. DynamoDB y acceso a datos

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-009 | Configurar DynamoDB Local mediante Docker. | TASK-002 | compose/scripts/test setup | Arranque/readiness/limpieza reproducibles; transacción local smoke. | REQ-DATA-001/003 |
| TASK-010 | Implementar catálogo de tipos, claves, shards y codecs. | TASK-009 | `packages/data-access/model` | Round-trip y snapshots de todos los tipos; ninguna PII directa en claves. | REQ-DATA-005/006 |
| TASK-011 | Crear tabla, GSI1/GSI2 y settings On-Demand mediante CDK. | TASK-006, TASK-010 | DynamoDB stack | Assertions de PK/SK, GSIs sparse KEYS_ONLY, cifrado, separación y PITR production. | REQ-DATA-001/006, REQ-SEC-004 |
| TASK-012 | Crear adaptador AWS SDK v3 y repositorio base. | TASK-009, TASK-010 | clients/repositories | Config local/remota segura; paginación, BatchGet y errores probados. | REQ-DATA-002/007 |
| TASK-013 | Implementar primitivas persistentes de idempotencia. | TASK-012 | idempotency repository/service | Misma clave/carga devuelve resultado; carga distinta falla; TTL solo efímero. | REQ-DATA-003/004 |
| TASK-014 | Implementar perfiles, Cognito y lookups HMAC de email/nombre. | TASK-012 | user repository/search tokens | Alta/actualización transaccional, unicidad, rotación y consultas por estado/búsqueda probadas. | REQ-AUTH-002, REQ-DATA-003/005 |
| TASK-015 | Implementar repositorios de membresías y pagos con vistas. | TASK-012, TASK-013 | membership/payment repositories | Puntero/vencimiento/estado/fecha y correcciones transaccionales probados. | REQ-MEMBER-002/003, REQ-PAY-001/002, REQ-DATA-003 |
| TASK-016 | Implementar repositorios de clases y reservas. | TASK-012, TASK-013 | class/reservation repositories | Acceso por fecha/entrenador/alumno/clase y condiciones base probados. | REQ-CLASS-001/002, REQ-BOOKING-001/002 |
| TASK-017 | Implementar galería, notificación, settings y auditoría; prohibir Scan normal. | TASK-012 | repositories + command guard | Todos los patrones restantes pasan; test falla ante `ScanCommand`. | REQ-GALLERY-001/002, REQ-NOTIFY-001/002, REQ-ADMIN-002, REQ-DATA-002 |

## Fase 5. Autenticación

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-018 | Definir Cognito y proveedores sociales por ambiente. | TASK-006, TASK-014 | auth stack/config | Callbacks/atributos/IAM sintetizados; sin secretos. | REQ-AUTH-001, REQ-SEC-003/004 |
| TASK-019 | Implementar sesión/callback y alta idempotente `PENDING`. | TASK-005, TASK-018 | auth routes/service | state/nonce/PKCE/cookies y callback repetido probados. | REQ-AUTH-001/002, REQ-DATA-003/004 |
| TASK-020 | Implementar finalización de perfil y logout. | TASK-019 | onboarding/profile | Campos, ownership y expiración E2E. | REQ-AUTH-002, REQ-MEMBER-001 |

## Fase 6. Autorización

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-021 | Codificar matriz de permisos y guards deny-by-default. | TASK-014, TASK-019 | domain/authz | Matriz por rol/estado/propiedad probada. | REQ-SEC-001, REQ-AUTH-003 |
| TASK-022 | Aplicar scoping de repositorios y rate limits. | TASK-021 | repositories/rate limit | Tests IDOR, límites y no enumeración. | REQ-SEC-001/002 |

## Fase 7. Sitio público

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-023 | Construir páginas públicas y contenido configurable. | TASK-004, TASK-017 | public routes/components | Contenido/enlaces, responsive y a11y E2E. | REQ-PUBLIC-001 |
| TASK-024 | Optimizar SEO, caché, imágenes y rendimiento. | TASK-023 | metadata/cache | Objetivos NFR medidos; caché no expone privados. | REQ-PUBLIC-001, REQ-SEC-002 |

## Fase 8. Gestión de alumnos

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-025 | Crear perfil propio con lista blanca editable. | TASK-020, TASK-022 | `/me/profile` | Ownership y campos protegidos probados. | REQ-MEMBER-001, REQ-SEC-001 |
| TASK-026 | Crear búsquedas/listados y revisión de solicitudes. | TASK-021, TASK-025 | `/admin/students` | Estado/email/nombre/pending sin Scan; transiciones y permisos probados. | REQ-AUTH-003, REQ-ADMIN-001/002, REQ-DATA-002 |

## Fase 9. Planes y membresías

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-027 | CRUD lógico de planes. | TASK-015, TASK-021 | plan service/UI | Validaciones, historial y permisos probados. | REQ-MEMBER-002 |
| TASK-028 | Gestionar membresías, puntero activo y vigencia/mora. | TASK-027 | membership service/UI | Transacciones, estados y bordes timezone probados. | REQ-MEMBER-002/003, REQ-DATA-003 |
| TASK-029 | Mostrar membresía propia y reportes por vencimiento/estado. | TASK-028 | portal/admin queries | Queries GSI paginadas/revalidación; IDOR y fechas probados. | REQ-MEMBER-001/003, REQ-DATA-002 |

## Fase 10. Pagos

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-030 | Registrar pagos idempotentes y comprobante privado. | TASK-015, TASK-021, TASK-028 | payment service/UI/S3 | Canónico/vistas/auditoría atómicos; upload firmado probado. | REQ-PAY-001, REQ-DATA-003/004 |
| TASK-031 | Historial propio y consultas administrativas fecha/estado. | TASK-030 | payment queries/UI | Paginación, ownership y cero Scan probados. | REQ-PAY-003, REQ-DATA-002 |
| TASK-032 | Anulación, ajuste y compensación inmutables. | TASK-030 | correction service/UI | Original persiste; solo ADMIN; vínculos/auditoría probados. | REQ-PAY-002, REQ-ADMIN-002 |

## Fase 11. Clases

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-033 | CRUD lógico de entrenadores y tipos de clase. | TASK-016, TASK-021 | services/UI | Referencias, permisos y soft delete probados. | REQ-CLASS-001 |
| TASK-034 | Crear/editar sesiones y claves de fecha/entrenador. | TASK-033 | session service/UI | Capacidad/hora/vistas/versión/auditoría probadas. | REQ-CLASS-001/002 |
| TASK-035 | Cancelar sesión y propagar cancelaciones idempotentes. | TASK-034 | cancellation worker/service | Bloqueo inmediato; lotes reanudables y auditoría probados. | REQ-CLASS-002, REQ-BOOKING-003 |

## Fase 12. Reservas

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-036 | Implementar reserva atómica con `TransactWriteItems`. | TASK-022, TASK-028, TASK-034 | booking service | Clase/alumno/membresía/duplicado/cupo/idempotencia en una transacción. | REQ-BOOKING-001/002/005, REQ-DATA-003/004 |
| TASK-037 | Implementar cancelación atómica y reconstrucción de contador. | TASK-036 | cancellation/reconciliation | Doble cancelación, contador positivo, repair por partición y auditoría probados. | REQ-BOOKING-003, REQ-DATA-003 |
| TASK-038 | Probar concurrencia, reintentos y fallos parciales. | TASK-036, TASK-037 | integration/load tests | N+M confirma ≤N; conflicto/timeout/replay no duplica ni desincroniza. | REQ-BOOKING-002/005, REQ-DATA-003/004 |
| TASK-039 | Crear UI de clases, cupos, futuras e historial. | TASK-036, TASK-037 | portal | Responsive; datos propios y flujo E2E. | REQ-BOOKING-004 |

## Fase 13. Galería

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-040 | Crear S3/SQS y carga privada segura. | TASK-007, TASK-017, TASK-021 | stacks/upload API | IAM/cifrado/límites/URL firmada probados. | REQ-GALLERY-001, REQ-SEC-003 |
| TASK-041 | Procesar derivados y marca de agua idempotente. | TASK-040 | image worker | MIME/EXIF/tamaños/reintento/corrupción probados. | REQ-GALLERY-001/003, REQ-DATA-004 |
| TASK-042 | Consentimiento, publicación/ocultamiento y feed público. | TASK-041, TASK-023 | gallery service/UI | Vista pública transaccional; original privado; caché probada. | REQ-GALLERY-002/003 |

## Fase 14. Notificaciones

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-043 | Crear adaptador SES, plantillas, lease e intentos. | TASK-017, TASK-007 | notification service/stack | Fake/sandbox; éxito/error/rebote/retry sanitizados. | REQ-NOTIFY-002, REQ-SEC-003 |
| TASK-044 | Job diario por índice de vencimiento y dedupe persistente. | TASK-028, TASK-043 | scheduler/Lambda | Query GSI fecha, revalidación fuerte y doble ejecución probadas. | REQ-NOTIFY-001/002, REQ-DATA-002/004 |

## Fase 15. Administración

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-045 | Integrar dashboard y vistas administrativas. | TASK-026, TASK-029, TASK-032, TASK-035, TASK-042 | `/admin` | Permisos, paginación, responsive y E2E por rol. | REQ-ADMIN-001 |
| TASK-046 | Administrar settings con versión/condiciones. | TASK-017, TASK-021, TASK-045 | settings service/UI | Solo ADMIN; conflicto/auditoría/sin secretos. | REQ-ADMIN-001/002 |

## Fase 16. Seguridad, auditoría y operación

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-047 | Completar auditoría por entidad y actor/fecha. | TASK-032, TASK-035, TASK-046 | audit service/UI | Append-only, GSI2, redacción y permisos probados. | REQ-ADMIN-002, REQ-SEC-003 |
| TASK-048 | Aplicar headers, CSRF, límites, WAF justificado y hardening. | TASK-008, TASK-022, TASK-040 | security config/stacks | Checklist OWASP, tests y assertions IAM exactas. | REQ-SEC-002/003 |
| TASK-049 | Configurar PITR, deletion protection, retención y restore runbook. | TASK-011 | DynamoDB stack/runbook | Production RETAIN/PITR; restauración a tabla nueva simulada. | REQ-DATA-001, REQ-SEC-004 |
| TASK-050 | Configurar alarmas DynamoDB, límites On-Demand, tags y budgets. | TASK-011 | monitoring stack | Consumo/throttle/error/conflict alarmados; costos atribuibles. | REQ-DATA-001/006 |
| TASK-051 | Implementar reconciliaciones y backfill segmentado seguro. | TASK-015, TASK-016, TASK-047 | ops tools/runbooks | Checkpoints, idempotencia, límite y auditoría; no corre en request. | REQ-DATA-003/007 |

## Fase 17. Pruebas

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-052 | Consolidar unitarias/integración DynamoDB y fixtures. | TASK-047, TASK-051 | test suites | Estados/permisos/transacciones/paginación y ausencia de Scan reproducibles. | Todos |
| TASK-053 | Probar consultas administrativas, E2E, a11y, rendimiento y carga. | TASK-052 | Playwright/load tests | Los 25 patrones y negativos pasan; métricas NFR documentadas. | REQ-DATA-002, REQ-BOOKING-002, REQ-SEC-001/002 |

## Fase 18. CI/CD

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-054 | CI con DynamoDB Local y controles de calidad/IaC. | TASK-006, TASK-052 | `.github/workflows/ci.yml` | lint/typecheck/test/build/synth/scans y no-Scan obligatorios. | REQ-DATA-002, REQ-SEC-003/004 |
| TASK-055 | Preparar CD con OIDC/approvals para development, sin desplegar. | TASK-054 | workflow/IAM | Dry-run, tabla/índices/IAM exactos; deploy requiere aprobación. | REQ-SEC-003/004 |

## Fase 19. Despliegue de development

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-056 | Revisar diff, costos y readiness de development. | TASK-053, TASK-055 | checklist/runbooks | Región, límites, IdPs/SES, tabla separada y aprobación; sin deploy. | REQ-DATA-001/006, REQ-SEC-004 |
| TASK-057 | Desplegar development solo con autorización y smoke test real. | TASK-056 + autorización | stacks/workflow | Todos los patrones, consistencia GSI, IAM, alarmas y rollback verificados. | Todos |

## Fase 20. Documentación final

| ID | Objetivo | Dependencias | Archivos/módulos | Finalización y pruebas | Requisitos |
|---|---|---|---|---|---|
| TASK-058 | Actualizar arquitectura, catálogo de claves y runbooks. | TASK-057 | README/docs | Enlaces, comandos y ejemplos validados por colaborador nuevo. | Todos |
| TASK-059 | Cerrar trazabilidad MVP y readiness production. | TASK-058 | matriz/release notes | Cada REQ tiene evidencia/excepción; riesgos production abiertos. | Todos |

## Criterio global

Ninguna fase de despliegue comienza con decisiones críticas pendientes de región, privacidad, identidad, correo, presupuesto o recuperación. Completar una tarea no autoriza la siguiente ni un despliegue. Las operaciones normales nunca dependen de `Scan`.
